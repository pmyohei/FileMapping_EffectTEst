<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mcv_map"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="24dp"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:theme="@style/Theme.MaterialComponents.Light"
    app:cardCornerRadius="0dp"
    app:strokeColor="@color/transparent_4_black"
    app:strokeWidth="1dp"
    app:cardElevation="4dp"
    >

    <TextView
        app:autoSizeMinTextSize="12sp"
        app:autoSizeMaxTextSize="18sp"
        app:autoSizeTextType="uniform"
        android:id="@+id/tv_mapName"
        android:layout_width="match_parent"
        android:layout_height="24dp"
        android:layout_marginTop="72dp"
        android:layout_marginBottom="72dp"
        android:gravity="center"
        android:text="@string/sample"
        android:textSize="18sp"
        android:textColor="@color/black" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:gravity="end"
        android:layout_marginBottom="8dp"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/ib_edit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:background="@color/transparent_0_white"
            app:srcCompat="@drawable/baseline_edit_gray_24" />

        <ImageButton
            android:id="@+id/ib_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:background="@color/transparent_0_white"
            app:srcCompat="@drawable/baseline_delete_gray_24" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>